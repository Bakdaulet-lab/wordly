# This file configures the analyzer for the Wordly project.
# See: https://dart.dev/guides/language/analysis-options
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat missing return types and unused imports as warnings
    missing_return: warning
    unused_import: warning
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "build/**"

linter:
  rules:
    # ── Style ──────────────────────────────────────────────────────────
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    sort_child_properties_last: true
    use_super_parameters: true
    require_trailing_commas: true

    # ── Safety ─────────────────────────────────────────────────────────
    avoid_print: true
    avoid_relative_lib_imports: true
    cancel_subscriptions: true
    close_sinks: true
    unawaited_futures: false          # We intentionally fire-and-forget in some places
    prefer_void_to_null: true
    use_key_in_widget_constructors: true
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true

    # ── Documentation ──────────────────────────────────────────────────
    slash_for_doc_comments: true

    # ── Effective Dart ─────────────────────────────────────────────────
    annotate_overrides: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    unnecessary_this: true
    unnecessary_new: true
    unnecessary_const: true
    unnecessary_string_interpolations: true
    prefer_interpolation_to_compose_strings: true
    prefer_collection_literals: true
    avoid_init_to_null: true
    empty_constructor_bodies: true
